Build "Gamble Chess" (working title "Gambit"), a chess variant where every 5 moves each player may spin a wheel to either grant themselves a random buff or inflict a random nerf on their opponent (e.g. buffs: extra turn, shield a piece from capture, revive a captured piece, instant-promote a pawn, swap two of your pieces, block the opponent's next nerf, undo their last move, bonus spin; nerfs: skip a turn, freeze a piece, lose a random pawn, downgrade the queen to rook movement, delay their next spin cycle, force a pawn to advance, shuffle two of their pieces). It should support four modes: quick play vs. a bot, local pass-and-play, fully custom matches (configurable rules/buff pool/turn interval), and online multiplayer between two people. Use chess.js for core move legality/check/checkmate handling, and layer the buff/nerf effects on top by manipulating the board state directly, with safety checks so no effect can leave a king in illegal check. The bot should be a simple minimax/alpha-beta AI with adjustable depth for difficulty. For online play, since this will be hosted for free on Cloudflare Pages (static frontend only, no server of its own), use a Cloudflare Worker with a Durable Object per game room as the backend — Durable Objects are free-tier eligible (no $5/mo plan required) and give strongly consistent state, which is what lets you say "the move doesn't register client-side until the server confirms it for both players": each client sends its move to the Worker, the Worker validates and updates the room's authoritative state, and both clients poll every couple seconds for the confirmed state (this gives a few seconds of lag, which is fine). What I'll need to do myself: create a free Cloudflare account, install the Wrangler CLI and run wrangler deploy to publish the Worker/Durable Object, connect the repo to Cloudflare Pages for the frontend build, and set the frontend's API base URL to point at the deployed Worker.

I don't want to install anything myself.

Reconciled browser-based deployment without local installations
Reconciled browser-based deployment without local installations

Got it — everything stays in the browser then. Updated summary:

Build "Gamble Chess" (working title "Gambit"), a chess variant where every 5 moves each player may spin a wheel to either grant themselves a random buff or inflict a random nerf on their opponent (e.g. buffs: extra turn, shield a piece from capture, revive a captured piece, instant-promote a pawn, swap two of your pieces, block the opponent's next nerf, undo their last move, bonus spin; nerfs: skip a turn, freeze a piece, lose a random pawn, downgrade the queen to rook movement, delay their next spin cycle, force a pawn to advance, shuffle two of their pieces). It should support four modes: quick play vs. a bot, local pass-and-play, fully custom matches (configurable rules/buff pool/turn interval), and online multiplayer between two people. Use chess.js for core move legality/check/checkmate handling, and layer the buff/nerf effects on top by manipulating the board state directly, with safety checks so no effect can leave a king in illegal check. The bot should be a simple minimax/alpha-beta AI with adjustable depth for difficulty. For online play, host the static frontend on Cloudflare Pages and back it with a Cloudflare Worker + Durable Object (one per game room) — both are free-tier eligible, and the Durable Object gives strongly consistent state so a move only registers client-side once the Worker confirms it for both players; clients poll the Worker every couple seconds, giving a few seconds of acceptable lag. Everything here — writing the code, running builds, and deploying — should be done by the Replit agent inside Replit's own cloud environment, not on your machine. The only things you personally need to do, entirely through web browsers with nothing installed locally: create a free Cloudflare account, and in the Cloudflare dashboard connect your GitHub repo to Cloudflare Pages (for the frontend) and Cloudflare Workers (for the backend) so they auto-deploy on push — Replit can push the code to GitHub for you, and Cloudflare's dashboard handles the build/deploy from there without any CLI on your end.